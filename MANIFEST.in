# setuptools-scm adds every git-tracked file to the sdist by default.
# Prune the repo-only files that are of no use to a downstream consumer.
# README.md / LICENSE are included automatically (via `readme =` and
# license-file handling) and src/pylsl/lib/* via [tool.setuptools.package-data].
prune .github
prune test
exclude .gitignore
